[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "zombi2_core"
version = "0.1.0"
description = "Optional Rust fast-path engine for ZOMBI2"
requires-python = ">=3.10"

[tool.maturin]
module-name = "zombi2_core"
