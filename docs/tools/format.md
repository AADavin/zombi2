<!-- Single source: this page IS the `format` section of the manual's Appendix C, included verbatim so
     the book and the site never drift. Correct the appendix; both update. The section starts below the
     appendix's own `## format` heading, so the page heading is the nav title. -->
--8<-- "manual/book/appendix-c.md:format"
